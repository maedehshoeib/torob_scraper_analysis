# داشبورد جغرافیایی بازار ترب

## معرفی پروژه

این پروژه یک داشبورد جغرافیایی تعاملی برای تحلیل داده‌های بازار ترب است. این داشبورد با استفاده از داده‌های فروشگاه‌ها و محصولات، اطلاعات مفیدی درباره توزیع جغرافیایی فروشگاه‌ها، نوع فروشگاه‌ها (آنلاین، آفلاین، ترکیبی) و تحلیل‌های دیگر ارائه می‌دهد.

## ویژگی‌ها

### 🗺️ نقشه تعاملی
- نمایش توزیع جغرافیایی فروشگاه‌ها در شهرهای ایران
- نشانگرهای دایره‌ای با اندازه متناسب با تعداد فروشگاه‌ها
- قابلیت کلیک برای مشاهده اطلاعات جزئی هر شهر

### 📊 داشبورد تحلیلی
- کارت‌های آماری با اطلاعات کلیدی (تعداد فروشگاه‌ها، شهرها، استان‌ها)
- نمودارهای توزیع انواع فروشگاه‌ها
- تحلیل شهرهای برتر از نظر تعداد فروشگاه
- به‌روزرسانی داده‌ها به صورت بلادرنگ

### 🔍 جستجو و فیلتر
- قابلیت جستجو بر اساس شهر، استان و نوع فروشگاه
- فیلترهای پیشرفته برای تحلیل دقیق‌تر

## معماری پروژه

### Backend (FastAPI)
- APIهای RESTful برای پردازش داده‌ها
- تحلیل داده‌های ۱۶۵,۰۰۰+ فروشگاه
- قابلیت‌های تحلیل جغرافیایی و تجمیع داده‌ها
- پشتیبانی از CORS برای ارتباط با فرانت‌اند

### Frontend (React)
- نقشه‌های تعاملی با استفاده از Leaflet
- کامپوننت‌های حرفه‌ای با Material-UI
- نمودارها و تحلیل‌ها با استفاده از Recharts
- طراحی واکنش‌گرا برای دستگاه‌های مختلف
- پشتیبانی از زبان فارسی (راست‌چین)

## نحوه اجرا

### راه‌اندازی سریع
```bash
cd /home/maede/Projects/torob_analysis/torob_dashboard
chmod +x start.sh
./start.sh
```

### راه‌اندازی دستی

#### Backend (ترمینال ۱)
```bash
cd backend
pip install -r requirements.txt
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

#### Frontend (ترمینال ۲)
```bash
cd frontend
npm install
npm start
```

## نقاط دسترسی

- **داشبورد**: http://localhost:3000
- **API Backend**: http://localhost:8000
- **مستندات API**: http://localhost:8000/docs

## ساختار فایل‌ها
```
torob_dashboard/
├── backend/
│   ├── main.py              # اپلیکیشن FastAPI
│   └── requirements.txt     # وابستگی‌های پایتون
├── frontend/
│   ├── public/
│   │   └── index.html       # قالب HTML
│   ├── src/
│   │   ├── components/      # کامپوننت‌های React
│   │   ├── pages/           # صفحات داشبورد
│   │   ├── services/        # ارتباط با API
│   │   └── App.js           # اپلیکیشن اصلی
│   └── package.json         # وابستگی‌های Node.js
├── start.sh                 # اسکریپت راه‌اندازی خودکار
├── README.md               # این فایل
```

## داده‌ها

### داده‌های فروشگاه‌ها (`torob_shops.csv`)
- **۱۶۵,۰۰۲ فروشگاه** با اطلاعات پایه
- فیلدها: شناسه، نام، دامنه، شهر، نوع فروشگاه و غیره

### جزئیات فروشگاه‌ها (`shopinfo_detail.csv`)
- اطلاعات جزئی فروشگاه‌ها شامل استان‌ها
- اطلاعات تماس و آدرس‌ها
- جزئیات کسب‌وکار و وضعیت تأیید

### داده‌های محصولات (`shop_products.csv`)
- لیست محصولات با دسته‌بندی‌ها و قیمت‌ها
- ارتباط فروشگاه-محصول
- داده‌های تحلیل بازار

## تست API

### بررسی وضعیت سیستم
```bash
curl http://localhost:8000/api/health
```

### دریافت تحلیل کلی
```bash
curl http://localhost:8000/api/analytics/overview
```

### جستجوی فروشگاه‌ها
```bash
curl "http://localhost:8000/api/shops/search?city=تهران&limit=5"
```

## توسعه‌های آینده
- **به‌روزرسانی داده‌ها به صورت بلادرنگ**
- **فیلترهای پیشرفته‌تر** با محدوده‌های زمانی
- **قابلیت خروجی گرفتن** (CSV، PDF)
- **ابزارهای مقایسه داده‌ها**
- **ویژوالیزیشن‌های پیشرفته** (نقشه‌های حرارتی، تحلیل سری‌های زمانی)

---

*این پروژه برای تحلیل جامع داده‌های بازار ترب طراحی شده است.*